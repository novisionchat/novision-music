:root {
  --bg-amoled: #000000; 
  --bg-panel: #121212; 
  --bg-hover: #1f1f1f; 
  --bg-active: #282828;
  --accent: #FF2A54; 
  --accent-hover: #ff4d70; 
  --text-main: #ffffff; 
  --text-muted: #b3b3b3; 
  --border: #282828;
}

* { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Poppins', sans-serif; -webkit-tap-highlight-color: transparent; }

body { background-color: var(--bg-amoled); color: var(--text-main); height: 100vh; height: 100dvh; overflow: hidden; -webkit-user-select: none; user-select: none; }
input, textarea { -webkit-user-select: auto; user-select: auto; }

::-webkit-scrollbar { width: 5px; } 
::-webkit-scrollbar-track { background: transparent; } 
::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.2); border-radius: 10px; } 
::-webkit-scrollbar-thumb:hover { background: rgba(255,255,255,0.4); }

.layout-container { display: flex; height: 100vh; height: 100dvh; width: 100vw; background-color: var(--bg-amoled); position: relative; overflow: hidden; }

/* PC SIDEBAR */
.sidebar-desktop { width: 260px; background-color: var(--bg-panel); display: flex; flex-direction: column; padding: 20px 15px; gap: 30px; border-right: 1px solid var(--border); z-index: 10; }
.logo-area { display: flex; align-items: center; gap: 12px; color: white; padding: 0 10px; }
.nav-item { display: flex; align-items: center; gap: 15px; padding: 12px 10px; color: var(--text-muted); text-decoration: none; border-radius: 8px; font-weight: 600; font-size: 15px; transition: all 0.2s; }
.nav-item:hover { color: white; background: var(--bg-hover); }
.nav-item.active { color: white; background: var(--bg-active); }

/* MAIN CONTENT (MODALLARIN ALTA DÜŞMEMESİ İÇİN Z-INDEX HAPSİ KALDIRILDI) */
.main-content { flex: 1; background: linear-gradient(180deg, #1f1f1f 0%, var(--bg-amoled) 300px); overflow-y: auto; padding: 20px 30px; padding-bottom: 120px; }

.top-bar { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
.user-profile-btn { width: 38px; height: 38px; border-radius: 50%; overflow: hidden; cursor: pointer; border: 2px solid transparent; transition: 0.2s; background: var(--bg-hover); }
.user-profile-btn:hover { border-color: var(--accent); transform: scale(1.05); }
.user-profile-btn img { width: 100%; height: 100%; object-fit: cover; }

/* MOBIL ALT MENÜ */
.mobile-bottom-nav { display: none; }

/* --- PLAYER BAR --- */
.player-bar-container { position: fixed; bottom: 0; left: 0; right: 0; height: 90px; background: var(--bg-amoled); border-top: 1px solid var(--border); z-index: 100; display: flex; justify-content: space-between; align-items: center; padding: 0 15px; }
.now-playing-info { display: flex; align-items: center; gap: 15px; width: 30%; min-width: 180px; transition: 0.2s; }
.now-playing-info.clickable:hover .track-title { text-decoration: underline; }
.track-thumb { width: 56px; height: 56px; border-radius: 6px; object-fit: cover; background-color: var(--bg-hover); }
.track-details { display: flex; flex-direction: column; overflow: hidden; white-space: nowrap; }
.track-title { font-size: 14px; font-weight: 600; color: white; text-overflow: ellipsis; overflow: hidden; }
.track-artist { font-size: 12px; color: var(--text-muted); text-overflow: ellipsis; overflow: hidden; }

.player-controls { flex: 1; display: flex; flex-direction: column; align-items: center; max-width: 45%; }
.main-buttons { display: flex; align-items: center; gap: 20px; margin-bottom: 5px; }
.icon-btn { background: transparent; border: none; color: var(--text-muted); cursor: pointer; transition: all 0.2s; display: flex; align-items: center; justify-content: center; }
.icon-btn:hover { color: white; transform: scale(1.1); }
.play-pause-btn { flex-shrink: 0; width: 40px; height: 40px; border-radius: 50%; background-color: white; border: none; cursor: pointer; display: flex; align-items: center; justify-content: center; transition: all 0.2s; }
.play-pause-btn:hover { transform: scale(1.05); background-color: #e0e0e0; }
.big-play { flex-shrink: 0; border-radius: 50%; aspect-ratio: 1; }
.player-actions { width: 30%; display: flex; justify-content: flex-end; }

.progress-container { display: flex; align-items: center; gap: 10px; width: 100%; max-width: 500px; margin-top: 5px; }
.time-text { font-size: 11px; color: var(--text-muted); min-width: 35px; text-align: center; }
.seek-bar-wrapper { width: 100%; height: 24px; display: flex; align-items: center; justify-content: center; cursor: pointer; }
.seek-bar { -webkit-appearance: none; width: 100%; height: 4px; border-radius: 2px; outline: none; cursor: pointer; transition: height 0.1s; background-color: #4d4d4d; }
.seek-bar-wrapper:hover .seek-bar { height: 6px; }
.seek-bar::-webkit-slider-thumb { -webkit-appearance: none; width: 12px; height: 12px; border-radius: 50%; background: #ffffff; opacity: 0; transition: opacity 0.2s; }
.seek-bar-wrapper:hover .seek-bar::-webkit-slider-thumb, .seek-bar:active::-webkit-slider-thumb { opacity: 1; }

/* --- DETAY PANELİ --- */
.now-playing-panel { position: relative; width: 0; background-color: var(--bg-panel); border-left: 1px solid var(--border); display: flex; flex-direction: column; transition: width 0.3s cubic-bezier(0.4, 0, 0.2, 1), padding 0.3s; overflow: hidden; z-index: 10; padding: 0; }
.now-playing-panel.open { width: 340px; padding: 20px; padding-bottom: 110px; }

.ambient-bg { position: absolute; top: -50%; left: -50%; width: 200%; height: 200%; background-size: cover; background-position: center; filter: blur(60px) saturate(250%) brightness(0.6); opacity: 0.8; z-index: 0; pointer-events: none; transition: background-image 1s ease-in-out; }
.ambient-overlay { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: linear-gradient(180deg, rgba(0,0,0,0.2) 0%, rgba(18,18,18,0.9) 60%, rgba(18,18,18,1) 100%); z-index: 0; pointer-events: none; }
.panel-header, .panel-scroll-area { position: relative; z-index: 1; }

.panel-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; width: 100%; flex-shrink: 0; }
.panel-header-title { font-weight: bold; font-size: 15px; color: var(--text-muted); margin: 0 auto; }

.panel-scroll-area { flex: 1; overflow-y: auto; display: flex; flex-direction: column; min-height: 0; }
.panel-scroll-area::-webkit-scrollbar { display: none; }

.panel-artwork-container { position: relative; width: 100%; max-width: 350px; aspect-ratio: 1; margin: 0 auto 20px auto; border-radius: 8px; overflow: hidden; box-shadow: 0 10px 40px rgba(0,0,0,0.5); flex-shrink: 0; background-color: black; }

/* YOUTUBE ENGINE VE IFRAME ESNETME MANTIĞI */
.youtube-react-wrapper { 
  width: 100% !important; 
  height: 100% !important; 
  position: relative !important;
  display: block !important;
}
iframe.youtube-video-fill { 
  position: absolute !important;
  top: 0 !important;
  left: 0 !important;
  width: 100% !important; 
  height: 100% !important; 
  border: none !important; 
}

.panel-artwork { width: 100%; height: 100%; object-fit: cover; position: absolute; top: 0; left: 0; }

.panel-info { text-align: left; margin-bottom: 20px; flex-shrink: 0; }
.panel-title { font-size: 22px; font-weight: bold; color: white; margin-bottom: 4px; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
.panel-artist { font-size: 15px; color: rgba(255,255,255,0.7); }

.panel-controls, .panel-actions { display: flex; flex-direction: column; width: 100%; flex-shrink: 0; }
.panel-main-buttons { justify-content: center; gap: 20px; margin-top: 15px; display: flex; }
.play-pause-btn.big { width: 56px; height: 56px; }
.panel-actions { flex-direction: row; justify-content: center; gap: 30px; margin-top: 25px; margin-bottom: 10px; opacity: 1; }

.lyrics-card { margin-top: 15px; background-color: rgba(0, 0, 0, 0.4); border-radius: 12px; padding: 20px; width: 100%; display: flex; flex-direction: column; transition: all 0.3s ease; overflow: hidden; flex-shrink: 0; }
.lyrics-card.expanded { flex: 1; margin-top: 0; background-color: transparent; padding: 0; height: 100%; }
.lyrics-header { font-size: 18px; font-weight: 700; color: white; margin-bottom: 20px; flex-shrink: 0; }
.lyrics-content { max-height: 40vh; overflow-y: auto; scroll-behavior: smooth; display: flex; flex-direction: column; gap: 10px; padding-bottom: 50%; position: relative; }
.lyrics-card.expanded .lyrics-content { max-height: 100%; flex: 1; }

.lyrics-content::-webkit-scrollbar { display: none; }
.lyrics-content { scrollbar-width: none; }

.lyric-line { font-size: 22px; font-weight: 700; color: rgba(255,255,255,0.3); cursor: pointer; transition: color 0.2s ease, transform 0.2s ease; line-height: 1.4; padding: 6px 0; transform-origin: left center; will-change: transform, color; }
.lyric-line:hover { color: rgba(255,255,255,0.7); }
.lyric-line.active { color: white; transform: scale(1.15); text-shadow: 0 0 10px rgba(255, 255, 255, 0.2); }
.lyrics-placeholder { text-align: center; color: rgba(255,255,255,0.4); font-size: 16px; margin: 30px 0; }

/* MODALLAR */
.modal-overlay { position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(0,0,0,0.85); backdrop-filter: blur(5px); z-index: 9999; display: flex; justify-content: center; align-items: center; }
.modal-content { background: #181818; padding: 35px 30px; border-radius: 16px; text-align: center; border: 1px solid var(--border); width: 90%; max-width: 400px; box-shadow: 0 20px 50px rgba(0,0,0,0.8); position: relative; }
.modal-close-btn { position: absolute; top: 15px; right: 15px; background: transparent; border: none; color: var(--text-muted); cursor: pointer; transition: 0.2s; }
.modal-close-btn:hover { color: white; transform: scale(1.1); }
.form-input { width: 100%; padding: 14px; background: #242424; border: 1px solid transparent; color: white; border-radius: 8px; outline: none; transition: 0.2s; font-size: 14px; margin-bottom: 15px; }
.form-input:focus { border-color: #fff; background: #2a2a2a; }
.primary-btn { background: var(--text-main); color: #000; border: none; padding: 14px; border-radius: 50px; cursor: pointer; width: 100%; font-weight: bold; font-size: 15px; transition: 0.2s; display: flex; justify-content: center; align-items: center; gap: 10px; }
.primary-btn:hover { transform: scale(1.03); background: #f0f0f0; }
.secondary-btn { padding: 8px 16px; background: transparent; border: 1px solid var(--border); color: var(--text-muted); border-radius: 50px; font-weight: bold; cursor: pointer; transition: 0.2s; }
.secondary-btn:hover { border-color: white; color: white !important; }
.switch-mode-text { margin-top: 20px; font-size: 13px; color: var(--text-muted); cursor: pointer; text-decoration: underline; transition: 0.2s; }
.switch-mode-text:hover { color: white; }
.error-text { color: #ff4d4d; font-size: 13px; margin-bottom: 15px; background: rgba(255, 77, 77, 0.1); padding: 10px; border-radius: 6px; }
.profile-avatar-large { width: 100px; height: 100px; border-radius: 50%; object-fit: cover; margin-bottom: 15px; border: 3px solid var(--border); }
.profile-name { font-size: 24px; font-weight: bold; margin-bottom: 5px; color: white; }
.profile-email { font-size: 14px; color: var(--text-muted); margin-bottom: 25px; }
.logout-btn { background: #2a2a2a; color: white; }
.logout-btn:hover { background: #3a3a3a; color: #ff4d4d; }

.playlist-selection-list { max-height: 220px; overflow-y: auto; margin-bottom: 20px; display: flex; flex-direction: column; gap: 8px; }
.playlist-select-item { display: flex; align-items: center; gap: 12px; padding: 12px; background: var(--bg-hover); border: 1px solid var(--border); border-radius: 8px; cursor: pointer; transition: 0.2s; text-align: left; }
.playlist-select-item:hover { background: #333; border-color: rgba(255,255,255,0.2); }

.library-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 5px; }
.add-playlist-btn { background: rgba(255,255,255,0.1); border-radius: 50%; padding: 8px; }
.add-playlist-btn:hover { background: var(--accent); }
.home-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); gap: 20px; }
.home-card { background: rgba(255,255,255,0.05); padding: 15px; border-radius: 8px; cursor: pointer; transition: 0.3s; text-align: center; border: 1px solid transparent; }
.home-card:hover { background: rgba(255,255,255,0.1); transform: translateY(-5px); border-color: rgba(255,255,255,0.2); }
.card-thumb-wrapper { position: relative; width: 100%; aspect-ratio: 1; margin-bottom: 15px; border-radius: 6px; overflow: hidden; box-shadow: 0 5px 15px rgba(0,0,0,0.5); }
.card-thumb-wrapper img { width: 100%; height: 100%; object-fit: cover; }
.card-title { font-weight: 600; font-size: 14px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; color: white; }
.card-subtitle { font-size: 12px; color: var(--text-muted); margin-top: 4px; }
.kebab-menu-container { position: absolute; z-index: 5; }
.kebab-btn { background: rgba(0,0,0,0.5); border-radius: 50%; width: 32px; height: 32px; opacity: 0; transition: 0.2s; backdrop-filter: blur(5px); }
.home-card:hover .kebab-btn { opacity: 1; }
.kebab-btn:hover { background: rgba(0,0,0,0.8); transform: scale(1.1); }
.custom-dropdown-menu { position: absolute; top: 100%; right: 0; background: #282828; border: 1px solid rgba(255,255,255,0.05); border-radius: 8px; box-shadow: 0 15px 40px rgba(0,0,0,0.8); display: flex; flex-direction: column; overflow: hidden; z-index: 100; min-width: 150px; animation: popIn 0.2s ease; margin-top: 5px; }
@keyframes popIn { from { opacity: 0; transform: translateY(-5px); } to { opacity: 1; transform: translateY(0); } }
.dropdown-item { padding: 12px 16px; font-size: 13px; color: var(--text-muted); cursor: pointer; transition: 0.2s; text-align: left; font-weight: 500; }
.dropdown-item:hover { background: rgba(255,255,255,0.1); color: white; }
.dropdown-item.active { color: var(--accent); font-weight: 700; }
.dropdown-item.delete { color: #ff4d4d; }
.dropdown-item.delete:hover { background: rgba(255, 77, 77, 0.1); }

.playlist-detail-page { display: flex; flex-direction: column; }
.back-btn-container { cursor: pointer; color: var(--text-muted); margin-bottom: 20px; transition: 0.2s; display: inline-block; width: max-content; }
.back-btn-container:hover { color: white; transform: translateX(-5px); }
.playlist-header-large { margin-bottom: 25px; }
.title-container { display: flex; align-items: center; gap: 15px; }
.playlist-huge-title { font-size: 48px; font-weight: 900; line-height: 1.1; color: white; word-break: break-word; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
.playlist-info-text { color: var(--text-muted); font-size: 14px; font-weight: 500; margin-top: 10px; }
.edit-name-container { display: flex; align-items: center; gap: 10px; }
.edit-name-input { background: transparent; border: none; border-bottom: 2px solid var(--accent); color: white; font-size: 40px; font-weight: 900; outline: none; width: 100%; max-width: 400px; padding-bottom: 5px; }
.playlist-action-bar { display: flex; align-items: center; gap: 20px; margin-bottom: 30px; }
.sort-dropdown-header { display: flex; align-items: center; gap: 5px; color: var(--text-muted); font-size: 13px; font-weight: 600; cursor: pointer; transition: 0.2s; padding: 5px 10px; border-radius: 6px; }
.sort-dropdown-header:hover { color: white; background: rgba(255,255,255,0.05); }
.sort-menu { right: 0; min-width: 170px; }

.dnd-row { position: relative; background: transparent; }
.dnd-row.dragging { background: #282828 !important; box-shadow: 0 10px 20px rgba(0,0,0,0.5); border-radius: 8px; z-index: 100; }
.dnd-row.edit-mode { background: rgba(255,255,255,0.02); }
.drag-handle { cursor: grab; padding: 10px; margin-left: -10px; display: flex; align-items: center; justify-content: center; }
.drag-handle:active { cursor: grabbing; }

.page-title { font-size: 28px; font-weight: bold; margin-bottom: 20px; color: white; }
.search-bar-container { display: flex; align-items: center; background-color: #242424; border-radius: 50px; padding: 12px 20px; gap: 15px; margin-bottom: 30px; border: 1px solid transparent; transition: all 0.2s; }
.search-bar-container:focus-within { border-color: white; background-color: #2a2a2a; }
.search-icon { color: var(--text-muted); }
.search-input { flex: 1; background: transparent; border: none; color: white; font-size: 15px; outline: none; }
.loading-container { display: flex; flex-direction: column; align-items: center; justify-content: center; margin-top: 50px; color: var(--text-muted); }
.loading-spinner { font-size: 40px; animation: spin 1s linear infinite; margin-bottom: 10px; }
@keyframes spin { 100% { transform: rotate(360deg); } }
.song-list { display: flex; flex-direction: column; gap: 5px; }
.song-row { display: flex; align-items: center; gap: 15px; padding: 10px 15px; border-radius: 8px; cursor: pointer; transition: background 0.2s; }
.song-row:hover { background-color: rgba(255,255,255,0.1); }
.song-row.active { background-color: rgba(255,255,255,0.15); }
.song-row.active .song-title { color: var(--accent); }
.song-thumb-container { position: relative; width: 48px; height: 48px; flex-shrink: 0; border-radius: 6px; overflow: hidden; }
.song-thumb { width: 100%; height: 100%; object-fit: cover; }
.play-overlay { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.5); display: flex; align-items: center; justify-content: center; opacity: 0; transition: opacity 0.2s; }
.song-row:hover .play-overlay { opacity: 1; }
.song-info { display: flex; flex-direction: column; overflow: hidden; flex: 1; }
.song-title { font-size: 15px; font-weight: 500; color: white; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; margin-bottom: 4px; }
.song-channel { font-size: 13px; color: var(--text-muted); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }

/* =====================================================================
   PC VE TABLET İÇİN TAM EKRAN (FULLSCREEN) DÜZENİ 
   VMIN ile hem yatayda hem dikeyde KUSURSUZ KARE ve RESPONSIVE ölçeklenme
   ===================================================================== */
@media screen and (min-width: 769px) {
  .now-playing-panel.fullscreen {
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw !important;
      height: 100vh !important;
      height: 100dvh !important;
      z-index: 9990 !important; /* Modal arkasında kalması için 9990 */
      background-color: var(--bg-amoled);
      padding: 30px 40px;
      border-left: none;
      display: flex;
      flex-direction: column;
      align-items: center;
  }
  .now-playing-panel.fullscreen .panel-header {
      width: 100%;
      max-width: 800px;
      margin: 0 auto;
      flex-shrink: 0;
  }
  .now-playing-panel.fullscreen .panel-scroll-area {
      max-width: 800px; 
      width: 100%;
      margin: 0 auto;
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;     
      justify-content: flex-start; /* DEĞİŞTİ: Üstten kesilmeyi durdurmak için flex-start yapıldı */
      gap: 30px;               
      padding-top: 10px;       /* Güvenli üst boşluk */
      padding-bottom: 40px;    /* Güvenli alt boşluk */
  }
  
  /* VMIN VE !IMPORTANT İLE RESPONSIVE KARE KAPSAYICI */
  .now-playing-panel.fullscreen .panel-artwork-container {
      width: 45vmin !important;   /* Ekranın en kısa kenarına göre esneyen kusursuz kare oran */
      height: 45vmin !important;  
      max-width: 80vw !important;  
      max-height: 80vw !important;
      min-width: 280px !important; 
      min-height: 280px !important;
      margin: 0 auto !important;          
      margin-top: auto !important; /* YENİ: Büyük ekranda dikey ortalamayı korur, küçük ekranda butonların arkasına girmeyi önler */
      aspect-ratio: 1 / 1 !important;     
      flex-shrink: 0 !important;          
      border-radius: 12px !important;
      box-shadow: 0 15px 50px rgba(0,0,0,0.6) !important;
      overflow: hidden !important;
      position: relative !important;
      background-color: black !important;
  }
  .now-playing-panel.fullscreen .panel-info {
      text-align: center;
      margin: 0 auto;
      width: 100%;
  }
  .now-playing-panel.fullscreen .panel-title {
      font-size: 32px;
      margin-bottom: 5px;
      justify-content: center;
  }
  .now-playing-panel.fullscreen .panel-controls {
      margin: 0 auto;
      width: 100%;
      max-width: 600px;
      display: flex;
      flex-direction: column;
      align-items: center;     /* PROGRESS BAR'I MERKEZE ÇEKER! */
  }
  .now-playing-panel.fullscreen .progress-container {
      margin: 0 auto;          /* EXTRA GÜVENLİK İÇİN MERKEZLEME */
      width: 100%;
  }
  .now-playing-panel.fullscreen .panel-main-buttons {
      margin-top: 25px;
      transform: scale(1.2);   /* BUTONLARI BİRAZ DAHA BÜYÜTÜYORUZ */
  }
  .now-playing-panel.fullscreen .panel-actions {
      margin: 0 auto !important;
      margin-bottom: auto !important; /* YENİ: Büyük ekranda dikey ortalamayı yukarıdan dengeler */
      justify-content: center;
      gap: 40px;
      width: 100%;
  }
  .now-playing-panel.fullscreen .lyrics-card {
      margin-top: 0;
      margin-bottom: auto !important; /* YENİ: Sözler açıkken dikey ortalamayı bozmaz */
      width: 100%;
      max-width: 600px;
  }
}

/* --- TABLET YATAY UYUMU (Ekranın Sıkışmasını Engeller) --- */
@media screen and (max-width: 1024px) and (min-width: 769px) {
  .sidebar-desktop { width: 220px; }
  .now-playing-panel.open:not(.fullscreen) { 
      position: absolute; 
      right: 0; 
      top: 0; 
      height: 100vh; 
      height: 100dvh; 
      z-index: 50; 
      box-shadow: -10px 0 50px rgba(0,0,0,0.8); 
  }
}

/* --- MOBİL UYUM --- */
@media screen and (max-width: 768px) {
  ::-webkit-scrollbar { display: none; } 
  * { scrollbar-width: none; } 

  .sidebar-desktop { display: none; } 
  .main-content { padding: 15px; padding-bottom: 150px; }

  .mobile-bottom-nav { display: flex; position: fixed; bottom: 0; left: 0; right: 0; height: 65px; background: rgba(18, 18, 18, 0.95); backdrop-filter: blur(15px); justify-content: space-around; align-items: center; z-index: 101; border-top: 1px solid var(--border); }
  .m-nav-item { display: flex; flex-direction: column; align-items: center; color: var(--text-muted); text-decoration: none; font-size: 11px; gap: 4px; font-weight: 500; }
  .m-nav-item.active { color: white; }
  
  .player-bar-container { position: fixed; bottom: 65px; height: 60px; width: calc(100% - 16px); left: 8px; border-radius: 8px; background: #2a2a2a; box-shadow: 0 4px 15px rgba(0,0,0,0.5); border: none; }
  .now-playing-info { width: 100%; }
  .track-thumb { width: 40px; height: 40px; }
  
  .player-controls { display: flex; position: absolute; top: -10px; left: 0; width: 100%; max-width: 100%; height: 20px; z-index: 10; }
  .player-bar-container .main-buttons, .player-bar-container .player-actions, .player-bar-container .time-text { display: none; } 
  .player-bar-container .progress-container { margin: 0; max-width: 100%; gap: 0; height: 100%; }
  .player-bar-container .seek-bar-wrapper { height: 100%; display: flex; align-items: center; }
  .player-bar-container .seek-bar { height: 2px; border-radius: 0; }
  .player-bar-container .seek-bar-wrapper:hover .seek-bar { height: 4px; }
  .player-bar-container .seek-bar::-webkit-slider-thumb { display: none !important; }

  .now-playing-panel { position: fixed; top: 100dvh; left: 0; right: 0; width: 100vw !important; height: 100dvh; z-index: 200; background-color: #121212; border-left: none; padding: 25px; }
  .now-playing-panel.open { top: 0; }
  
  /* Mobilde tam ekran butonu gizlenir çünkü zaten hep tam ekrandır */
  .toggle-fullscreen-btn { display: none !important; }
  
  .panel-artwork-container { max-width: min(85vw, 42vh); margin-bottom: 20px; }
  
  .panel-info { text-align: left; margin-bottom: 10px; }
  .panel-title { font-size: 24px; }

  .panel-controls { margin-top: auto; } 
  .panel-progress { margin-top: 10px; }
  .panel-progress .seek-bar-wrapper { height: 24px; align-items: center; }
  .panel-progress .time-text { display: block; font-size: 12px; }
  
  .now-playing-panel .main-buttons { gap: 30px; margin-top: 15px; margin-bottom: 10px; display: flex; }
  .panel-actions { justify-content: space-between; padding: 0 20px; margin-top: 15px; margin-bottom: 10px; opacity: 1; }

  .home-grid { grid-template-columns: repeat(auto-fill, minmax(130px, 1fr)); gap: 15px; }
  .kebab-btn { opacity: 1; } 
  .playlist-huge-title { font-size: 32px; }
  .edit-name-input { font-size: 28px; }
  .remove-song-btn { opacity: 1; color: #ff4d4d; }
  
  .search-bar-container { padding: 10px 15px; }
  .song-row { padding: 8px 10px; }
  .song-row:hover .play-overlay { opacity: 0; } 
}

@media screen and (max-width: 768px) {
  .now-playing-panel.open { padding-bottom: 25px !important; }
}

.search-chip { display: inline-flex; align-items: center; gap: 8px; background: rgba(255,255,255,0.1); padding: 8px 15px; border-radius: 50px; font-size: 13px; cursor: pointer; transition: 0.2s; border: 1px solid transparent; }
.search-chip:hover { background: rgba(255,255,255,0.2); border-color: rgba(255,255,255,0.3); }
.search-chip .icon-btn { color: rgba(255,255,255,0.5); transition: 0.2s; }
.search-chip:hover .icon-btn { color: white; }

.mode-switch { position: relative; display: flex; background: rgba(255,255,255,0.1); border-radius: 50px; padding: 4px; width: 140px; margin: 0 auto; box-shadow: inset 0 2px 10px rgba(0,0,0,0.5); }
.mode-bg { position: absolute; top: 4px; left: 4px; width: calc(50% - 4px); height: calc(100% - 8px); background: var(--bg-active); border-radius: 50px; transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1); z-index: 1; }
.mode-btn { flex: 1; background: transparent; border: none; color: var(--text-muted); font-size: 13px; font-weight: 600; z-index: 2; cursor: pointer; padding: 6px 0; transition: color 0.3s; position: relative; }
.mode-btn.active { color: white; }

.hidden-for-lyrics { position: absolute !important; width: 1px !important; height: 1px !important; opacity: 0 !important; pointer-events: none !important; overflow: hidden !important; z-index: -10 !important; margin: 0 !important; }

.big-play { background-color: var(--accent) !important; color: white !important; }
.big-play:hover { background-color: var(--accent-hover) !important; transform: scale(1.05); }

.playlist-search-box { display: flex; align-items: center; background-color: rgba(255,255,255,0.05); padding: 10px 15px; border-radius: 8px; margin-bottom: 20px; gap: 10px; border: 1px solid transparent; transition: 0.2s; }
.playlist-search-box:focus-within { border-color: rgba(255,255,255,0.2); background-color: rgba(255,255,255,0.08); }
.playlist-search-box input { flex: 1; background: transparent; border: none; color: white; outline: none; font-size: 14px; }
.playlist-search-box input::placeholder { color: var(--text-muted); }

/* Yan panelin ve içerisindeki elemanların yumuşakça kayarak açılıp daralması */
.sidebar-desktop {
  transition: width 0.2s cubic-bezier(0.4, 0, 0.2, 1), padding 0.2s !important;
}

/* Yan panel çalma listesi elemanlarının hover efekti */
.sidebar-playlist-item {
  transition: background-color 0.2s ease;
}
.sidebar-playlist-item:hover {
  background-color: var(--bg-hover) !important;
}

/* Scrollbar özelleştirmesi yan panel dropdown için */
.sidebar-playlists-dropdown::-webkit-scrollbar {
  width: 4px;
}
.sidebar-playlists-dropdown::-webkit-scrollbar-thumb {
  background: rgba(255, 255, 255, 0.1);
  border-radius: 4px;
}